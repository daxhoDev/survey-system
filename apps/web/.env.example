# Copy to .env and adjust. Documented in docs/sdd/05-configuration.md (CFG-09).

# Base URL of the API, without the /api/v1 path. Optional in development
# (default http://localhost:3000); required when building for production.
VITE_API_URL=http://localhost:3000
